{
  "name": "siaf-cloud-api",
  "version": "1.0.0",
  "main": "index.js",
  "bin": "bin.js",
  "license": "MIT",
  "scripts": {
    "start": "tsc --build --clean && tsc --build && node dist/server.js",
    "dev": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules --respawn src/server.ts",
    "build": "tsc",
    "typeorm": "ts-node-dev ./node_modules/typeorm/cli",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@types/express": "^4.17.11",
    "@types/uuid": "^8.3.0",
    "cors": "^2.8.5",
    "date-fns": "^2.21.3",
    "date-format": "^3.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "fs": "^0.0.1-security",
    "ini": "^2.0.0",
    "reflect-metadata": "^0.1.13",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  }
}
